version: 1
rules:
  - name: block-rm-recursive
    tool: Bash
    match:
      command: rm
      flags_all: [r, f]
    except:
      glob:
        mode: all
        patterns: ["/tmp/**"]
    action: deny
